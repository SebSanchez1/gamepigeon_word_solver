// App is the top-level component. Right now it just renders a placeholder
// so we can confirm the dev server and React are wired up correctly.
// Later steps will replace this with the actual word solver UI.
function App() {
  return (
    <div>
      <h1>GamePigeon Word Solver</h1>
      <p>App is running. More to come.</p>
    </div>
  )
}

export default App
